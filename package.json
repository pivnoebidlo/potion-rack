{
  "name": "potion-rack",
  "version": "0.1.2",
  "description": "Paint Manager for Miniatures",
  "author": {
    "name": "Potion Rack Team",
    "email": "potionrack@example.com"
  },
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.renderer.json",
    "start": "npm run build && electron .",
    "postinstall": "echo \"Skipping auto rebuild in CI\"",
    "rebuild": "npx @electron/rebuild -f -w better-sqlite3",
    "dist:mac": "npm run build && npm run rebuild && electron-builder --mac --arm64",
    "dist:win": "npm run build && npm run rebuild && electron-builder --win --x64",
    "dist:linux": "npm run build && npm run rebuild && electron-builder --linux --x64"
  },
  "build": {
    "appId": "com.potionrack.app",
    "productName": "Potion Rack",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "public/**/*",
      "!dist/**/*.map",
      "!dist/**/*.ts"
    ],
    "asarUnpack": [
      "**/*.node"
    ],
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.productivity",
      "hardenedRuntime": false,
      "gatekeeperAssess": false,
      "identity": null
    },
    "win": {
      "target": [
        "nsis",
        "portable"
      ]
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Graphics"
    }
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.4",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^22.0.0",
    "electron": "^35.7.5",
    "electron-builder": "^24.13.3",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "marked": "^18.0.3"
  }
}
